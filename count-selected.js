function countSelected(array) {
	let numberSelected = 0;
	for (let i = 0; i < array.length; i++) {
		if (array[i].selected) {
			numberSelected++;
		}
	}
	return numberSelected;
}

function countNumbers(...allNumbers) {
	allNumbers.forEach;
}

countNumbers(4, 5, 6, 7, 8, 9, 10);
